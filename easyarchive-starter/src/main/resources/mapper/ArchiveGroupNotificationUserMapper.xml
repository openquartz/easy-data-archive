<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.openquartz.easyarchive.starter.mapper.ArchiveGroupNotificationUserMapper">

    <resultMap id="BaseResultMap" type="com.openquartz.easyarchive.starter.model.entity.ArchiveGroupNotificationUser">
        <id column="id" property="id"/>
        <result column="group_id" property="groupId"/>
        <result column="user_id" property="userId"/>
        <result column="created_time" property="createdTime"/>
        <result column="updated_time" property="updatedTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.openquartz.easyarchive.starter.model.entity.ArchiveGroupNotificationUser"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ea_archive_group_notification_user (
            group_id, user_id
        ) VALUES (
            #{groupId}, #{userId}
        )
    </insert>

    <insert id="batchInsert">
        INSERT INTO ea_archive_group_notification_user (
            group_id, user_id
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.groupId}, #{item.userId})
        </foreach>
    </insert>

    <delete id="deleteByGroupId">
        DELETE FROM ea_archive_group_notification_user
        WHERE group_id = #{groupId}
    </delete>

    <select id="selectByGroupId" resultMap="BaseResultMap">
        SELECT id, group_id, user_id, created_time, updated_time
        FROM ea_archive_group_notification_user
        WHERE group_id = #{groupId}
        ORDER BY id DESC
    </select>

    <select id="selectUserIdsByGroupId" resultType="long">
        SELECT user_id
        FROM ea_archive_group_notification_user
        WHERE group_id = #{groupId}
        ORDER BY id DESC
    </select>

</mapper>
