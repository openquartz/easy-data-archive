services:
  mysql:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE:-easy_archive}
      MYSQL_USER: ${MYSQL_USER:-easyarchive}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-easyarchive123}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root123456}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docs/database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
      - ./easyarchive-starter/src/main/resources/db/migration/V1__init_archive_platform.sql:/docker-entrypoint-initdb.d/02-platform.sql:ro
      - ./easyarchive-starter/src/main/resources/db/migration/V2__add_archive_log_tables.sql:/docker-entrypoint-initdb.d/03-archive-log-tables.sql:ro
      - ./easyarchive-starter/src/main/resources/db/migration/V4__add_user_datasource_permissions.sql:/docker-entrypoint-initdb.d/04-user-datasource-permissions.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  backend:
    build:
      context: .
      dockerfile: easyarchive-starter/Dockerfile
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_DATABASE: ${MYSQL_DATABASE:-easy_archive}
      MYSQL_USER: ${MYSQL_USER:-easyarchive}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-easyarchive123}
      MYSQL_TIMEZONE: ${MYSQL_TIMEZONE:-Asia/Shanghai}

  frontend:
    build:
      context: ./easyarchive-ui
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "80:80"

volumes:
  mysql_data:
